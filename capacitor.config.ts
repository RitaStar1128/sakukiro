import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.manus.kaimonokiroku',
  appName: 'Kaimono Kiroku',
  webDir: 'dist/public'
};

export default config;
